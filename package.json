{
  "name": "niceelitechkaboom",
  "author": "niceEli",
  "description": "The Kaboom TS Game Engine (Hey atleast its not unity)",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && electron-builder",
    "buildWin": "tsc && vite build && electron-builder -w",
    "buildMac": "tsc && vite build && electron-builder -m",
    "buildGL": "tsc && vite build && electron-builder -l",
    "buildWeb": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/matter-js": "^0.19.5",
    "electron": "^28.1.0",
    "electron-builder": "^24.9.3",
    "typescript": "^5.3.3",
    "vite": "^5.0.10",
    "vite-plugin-electron": "^0.15.4",
    "vite-plugin-electron-renderer": "^0.14.5"
  },
  "main": "dist-electron/main.js",
  "dependencies": {
    "axios": "^1.6.2",
    "kaboom": "^3000.1.17",
    "kaboom-flex-ui": "^0.0.8",
    "matter-js": "^0.19.0"
  }
}
